# FitnessTrackerFrontend

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 20.3.9.

## Development server

To start a local development server, run:

```bash
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## Code scaffolding

Angular CLI includes powerful code scaffolding tools. To generate a new component, run:

```bash
ng generate component component-name
```

For a complete list of available schematics (such as `components`, `directives`, or `pipes`), run:

```bash
ng generate --help
```

## Building

To build the project run:

```bash
ng build
```

This will compile your project and store the build artifacts in the `dist/` directory. By default, the production build optimizes your application for performance and speed.

## Running unit tests

To execute unit tests with the [Karma](https://karma-runner.github.io) test runner, use the following command:

```bash
ng test
```

## Running end-to-end tests

For end-to-end (e2e) testing, run:

```bash
ng e2e
```

Angular CLI does not come with an end-to-end testing framework by default. You can choose one that suits your needs.

## Additional Resources

For more information on using the Angular CLI, including detailed command references, visit the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.












-------------------------------------------------------------------------------



PS C:\Users\\Desktop\Projekt-Seminar\project-seminar-programming> npx @angular/cli@20 new fitness-tracker-frontend --standalone --routing --style=scss
>>
Need to install the following packages:
@angular/cli@20.3.9
Ok to proceed? (y)      y

✔ Do you want to enable Server-Side Rendering (SSR) and Static Site Generation (SSG/Prerendering)? No
✔ Do you want to create a 'zoneless' application without zone.js? No
✔ Which AI tools do you want to configure with Angular best practices? https://angular.dev/ai/develop-with-ai None
CREATE fitness-tracker-frontend/angular.json (2722 bytes)
CREATE fitness-tracker-frontend/package.json (1147 bytes)
CREATE fitness-tracker-frontend/README.md (1544 bytes)
CREATE fitness-tracker-frontend/tsconfig.json (1026 bytes)
CREATE fitness-tracker-frontend/.editorconfig (331 bytes)
CREATE fitness-tracker-frontend/.gitignore (647 bytes)
CREATE fitness-tracker-frontend/tsconfig.app.json (444 bytes)
CREATE fitness-tracker-frontend/tsconfig.spec.json (422 bytes)
CREATE fitness-tracker-frontend/.vscode/extensions.json (134 bytes)
CREATE fitness-tracker-frontend/.vscode/launch.json (490 bytes)
CREATE fitness-tracker-frontend/.vscode/tasks.json (980 bytes)
CREATE fitness-tracker-frontend/src/main.ts (228 bytes)
CREATE fitness-tracker-frontend/src/index.html (321 bytes)
CREATE fitness-tracker-frontend/src/styles.scss (81 bytes)
CREATE fitness-tracker-frontend/src/app/app.spec.ts (705 bytes)
CREATE fitness-tracker-frontend/src/app/app.ts (319 bytes)
CREATE fitness-tracker-frontend/src/app/app.scss (0 bytes)
CREATE fitness-tracker-frontend/src/app/app.html (20464 bytes)
CREATE fitness-tracker-frontend/src/app/app.config.ts (412 bytes)
CREATE fitness-tracker-frontend/src/app/app.routes.ts (80 bytes)
CREATE fitness-tracker-frontend/public/favicon.ico (15086 bytes)
✔ Packages installed successfully.
    Directory is already under version control. Skipping initialization of git.
PS C:\Users\\Desktop\Projekt-Seminar\project-seminar-programming>

----------------------------------------------------------------------------
npm run build -- --watch
"To run build in "fitness-tracker-frontend" integrated terminal
"Watch rebuilds the files when something is changed"


To install dependencies
npm ci

Backend --> Über Main

Frontend:

cd fitness-tracker-frontend
npm start
